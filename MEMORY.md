# MEMORY

Cross-session log — the shift notes that survive context resets. One line per
fact; the full fact lives in its own file under the project's `memory/` dir.
Format: `- [Title](file.md) — hook`.

<!-- Add pointers below. Keep this index scannable; never put fact bodies here. -->
